import { Link } from "react-router-dom";

/**
 * items: [{ name, path }]  — path omitted (or null) on the last/current item.
 * Renders visible breadcrumb UI. Pair with breadcrumbSchema() from data/schema.js
 * in the page's SEO structuredData prop.
 */
export default function Breadcrumbs() {
  // Visible breadcrumb strip removed as requested for a cleaner, modern professional look
  return null;
}
